# ============================================================================
# FILE: app/views/deposits/landing.html.erb
# ============================================================================
<div class="container mx-auto px-4 py-8 max-w-2xl">
  <% if notice %>
    <div class="bg-green-50 border border-green-200 text-green-800 px-4 py-3 rounded mb-6">
      <%= notice %>
    </div>
  <% end %>

  <div class="bg-white shadow-lg rounded-lg p-8">
    <h1 class="text-3xl font-bold mb-6">Deposit Instructions</h1>
    
    <div class="mb-6">
      <h2 class="text-xl font-semibold mb-2">Round: <%= @round.name %></h2>
      <p class="text-gray-600">ROI: <%= @round.roi %>% | Jackpot: <%= @round.jackpot_percent %>%</p>
    </div>

    <div class="bg-blue-50 border border-blue-200 rounded-lg p-6 mb-6">
      <h3 class="font-semibold mb-2">Deposit Amount</h3>
      <p class="text-sm text-gray-600 mb-4">
        Please send between 
        <strong><%= number_with_precision(@round.btc_minimum_deposit, precision: 4) %> BTC</strong> and 
        <strong><%= number_with_precision(@round.btc_maximum_deposit, precision: 4) %> BTC</strong>
      </p>

      <h3 class="font-semibold mb-2">To This Address:</h3>
      <div class="bg-white border border-gray-300 rounded p-3 font-mono text-sm break-all">
        <%= @deposit.deposit_address %>
      </div>
      
      <button onclick="navigator.clipboard.writeText('<%= @deposit.deposit_address %>')" 
              class="mt-3 px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700">
        Copy Address
      </button>
    </div>

    <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4 mb-6">
      <h3 class="font-semibold text-yellow-800 mb-2">⚠️ Important Notes</h3>
      <ul class="list-disc list-inside text-sm text-yellow-700 space-y-1">
        <li>Your payout will be sent to: <strong class="font-mono"><%= @deposit.payout_address %></strong></li>
        <li>You will receive <%= 100 + @round.roi %>% of your deposit when the round pays out</li>
        <li>The round expires in <%= number_with_precision(@round.expiration_hours, precision: 1) %> hours after the last deposit</li>
        <li>Save this page or bookmark it to track your deposit</li>
      </ul>
    </div>

    <div class="flex gap-4">
      <%= link_to "View Round Details", @round, class: "flex-1 text-center px-4 py-2 bg-gray-600 text-white rounded hover:bg-gray-700" %>
      <%= link_to "Back to Rounds", root_path, class: "flex-1 text-center px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700" %>
    </div>
  </div>

  <div class="mt-6 text-center text-sm text-gray-500">
    <p>Once your transaction confirms, your deposit will automatically be processed.</p>
    <p class="mt-2">Minimum confirmations: 1</p>
  </div>
</div>

<script>
  // Auto-refresh page every 30 seconds to check for confirmation
  setTimeout(() => {
    location.reload();
  }, 30000);
</script>